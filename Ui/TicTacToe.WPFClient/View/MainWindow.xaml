<Window x:Class="MichaelKoch.TicTacToe.Ui.TicTacToe.WPFClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"                     
        xmlns:sd="clr-namespace:MichaelKoch.TicTacToe.Ui.TicTacToe.WPFClient.SampleData"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="TicTacToe" Height="650" Width="850">
    <d:Window.DataContext>
        <sd:MainWindowViewModelSampleDate/>
    </d:Window.DataContext>


    <DockPanel Background="{StaticResource TTTBackgroundDarkGrey}">

        <ContentControl Content="{Binding Path=MenuViewModel}"
                        ContentTemplate="{StaticResource MenuViewDataTemplate}"
                        DockPanel.Dock="Top"
                        x:Name="menuView"/>

        <Grid DockPanel.Dock="Left"
              Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="300"/>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>

            <ContentControl Content="{Binding Path=GameBoardViewModel}"
                            ContentTemplate="{StaticResource GameBoardViewDataTemplate}"
                            Grid.Row="0"
                            x:Name="gameBoardView"/>

            <ContentControl Content="{Binding Path=GameInfoViewModel}"
                            ContentTemplate="{StaticResource GameInfoViewDataTemplate}"
                            Grid.Row="1"
                            x:Name="gameInfoView"/>

        </Grid>

        <Grid DockPanel.Dock="Right"
              Width="auto"
              Height="Auto">
            

        </Grid>
        
    </DockPanel>
    
    
</Window>
